{
  "$schema": "./patterns.schema.json",
  "description": "Regex patterns for benchmarking zk-regex v1 vs v2",
  "patterns": [
    {
      "name": "body_hash",
      "description": "DKIM body hash extraction (complex real-world pattern)",
      "circuitName": "body_hash_regex",
      "availableInV1": true,
      "purpose": "Primary comparison metric for complex patterns"
    },
    {
      "name": "email_addr",
      "description": "Email address from To header (complex with capture groups)",
      "circuitName": "email_addr_regex",
      "availableInV1": true,
      "purpose": "Test capture group performance"
    },
    {
      "name": "subject_all",
      "description": "Full Subject header extraction",
      "circuitName": "subject_all_regex",
      "availableInV1": true,
      "purpose": "Medium complexity real-world header"
    },
    {
      "name": "simple",
      "description": "Simple a*b pattern for baseline measurements",
      "circuitName": "simple_regex",
      "availableInV1": false,
      "purpose": "Minimal baseline (6 transitions)"
    }
  ]
}
