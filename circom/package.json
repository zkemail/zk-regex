{
    "name": "@zk-email/zk-regex-circom",
    "version": "2.2.2-alpha.0",
    "license": "MIT",
    "description": "regex verification circuits in circom for common regexes, generated with the compiler in [zk-regex](https://github.com/zkemail/zk-regex/tree/main).",
    "scripts": {
        "test": "bun test --timeout 30000 circuits/tests/"
    },
    "dependencies": {
        "@zk-email/circuits": "6.4.1-alpha.0",
        "circomlib": "^2.0.5",
        "prettier": ""
    },
    "devDependencies": {
        "@types/bun": "latest",
        "chai": "^4.3.7",
        "circom_tester": "^0.0.24",
        "circomlibjs": "^0.1.2",
        "ffjavascript": "^0.3.1"
    },
    "engines": {
        "node": ">=18.0.0",
        "bun": ">=1.0.0"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
